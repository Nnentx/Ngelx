name: NgelX V50 R2 Live

on:
  push:
    branches: [ngelx-v50-r2-live]
    paths:
      - 'app/**'
      - 'firestore.rules'
      - 'cloudflare/worker/**'
      - '.github/workflows/build-v50-r2-live.yml'
  workflow_dispatch:

concurrency:
  group: ngelx-v50-r2-live
  cancel-in-progress: true

jobs:
  verify-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Kaynaklari al
        uses: actions/checkout@v4

      - name: R2 Worker saglik kontrolu
        run: |
          set -e
          curl --fail --silent --show-error             https://ngelx-media.alihancaglar76.workers.dev/health

      - name: Flutter kur
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Paketleri kur
        working-directory: app
        run: flutter pub get

      - name: Kod analizi
        working-directory: app
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Testleri calistir
        working-directory: app
        run: |
          if [ -d test ]; then
            flutter test
          else
            echo "Test klasoru yok; derleme kontrolune geciliyor."
          fi

      - name: Debug APK derle
        working-directory: app
        run: flutter build apk --debug

      - name: APK dosyasini yukle
        uses: actions/upload-artifact@v4
        with:
          name: NgelX-V50-R2-LIVE-APK
          path: app/build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error
          retention-days: 14
